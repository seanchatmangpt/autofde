[package]
name = "autofde-sentinel-ingress-harness"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
blake3 = "1.8"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[workspace]
