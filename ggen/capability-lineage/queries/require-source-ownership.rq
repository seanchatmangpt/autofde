PREFIX clc: <https://ggen.dev/ontology/capability-lineage-consumer#>
SELECT ?consumer WHERE {
  ?consumer a clc:Consumer .
  FILTER NOT EXISTS { ?consumer clc:sourceOwnership "ontology/autofde.ttl+ontology/source-bundle.txt" }
}
